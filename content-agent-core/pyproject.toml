[project]
name = "content-agent-core"
version = "0.1.0"
description = "Shared runtime helpers for the content agent toolchain"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
