# content-agent-core

Shared helpers used across the content agent workspace.

Current responsibilities:

- safe `.env` parsing for Python tooling
- shared `agent-browser` session helpers
- lightweight HTTP JSON/text fetching utilities
