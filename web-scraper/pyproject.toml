[project]
name = "web-scraper"
version = "0.1.0"
description = "Agent-friendly research article scraper with structured JSON output"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scrapling[fetchers]>=0.4.1",
    "beautifulsoup4>=4.12.3",
    "markitdown>=0.0.1a3",
]
