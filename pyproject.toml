[project]
name = "content-agent-tools"
version = "0.1.0"
description = "Workspace wrapper for the content-agent toolchain"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "content-agent-core",
    "gmail-reader",
    "web-scraper",
    "image-upload",
    "wiki-automation",
]
